<%= form_for(@scanner) do |f| %>
  <% if @scanner.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@scanner.errors.count, "error") %> prohibited this scanner from being saved:</h2>

      <ul>
      <% @scanner.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :modelo %><br />
    <%= f.text_field :modelo %>
  </div>
  <div class="field">
    <%= f.label :mark_id %><br />
    <%= collection_select(:scanner, :mark_id, Mark.order("nombre"), :id, :nombre ) %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
