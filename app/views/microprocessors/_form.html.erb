<%= form_for(@microprocessor) do |f| %>
  <% if @microprocessor.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@microprocessor.errors.count, "error") %> prohibited this microprocessor from being saved:</h2>

      <ul>
      <% @microprocessor.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :modelo %><br />
    <%= f.text_field :modelo %>
  </div>
  <div class="field">
    <%= f.label :nombre %><br />
    <%= f.text_field :nombre %>
  </div>
  <div class="field">
    <%= f.label :velocidad %><br />
    <%= f.text_field :velocidad %>
  </div>
  <div class="field">
    <%= f.label :mark_id %><br />
    <%= collection_select(:microprocessor, :mark_id, Mark.order("nombre"), :id, :nombre ) %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
