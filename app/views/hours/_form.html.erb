
<%= form_for(@hour) do |f| %>
  <% if @hour.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@hour.errors.count, "error") %> prohibited this hour from being saved:</h2>

      <ul>
      <% @hour.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
<% if permitted_to? :create, :hours  %>   
  <div class="field">
    <%= f.label :Empresa %><br />
    <%= collection_select(:hour, :company_id, Company.order("nombre"), :id, :nombre ) %>
  </div>
  <div class="field">
    <%= f.label :detalle %><br />
    <%= f.text_area :detalle %>
  </div>
  <div class="field">
    <%= f.label :horas %><br />
    <%= f.text_field :horas %>
  </div>
  <div class="field">
    <%= f.label :factura %><br />
    <%= f.text_field :factura %>
  </div>
  <div class="field">
    <%= f.label :fecha %><br />
<%= f.text_field :fecha, class: 'dp3' %>



</div>
  </div>
<% end %>
  <div class="field">
    <%= f.label :autorizado %><br />
    <%= f.check_box :autorizado %>
  </div>
  <div class="action">
    <%= f.submit "Crear Hora", class: 'btn btn-primary' %>
  </div>
<% end %>
